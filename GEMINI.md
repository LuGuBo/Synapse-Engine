# Synapse Engine Local Harness (Zero-Pollution Supreme Wrapper v2.0)

This workspace is managed by the **Synapse Engine Global Supreme Orchestrator v2.0**.
All master rules, cognitive constraints, Dark Factory adversarial pipelines, Graphify AST navigation guidelines, automatic UX/UI aesthetic triggers, and JIT Tri-Layer Skill pre-hooks are inherited from the global configuration and supreme orchestrator:
- `%USERPROFILE%\.gemini\GEMINI.md` (Global IDE Rules)
- `[Global Skills Dir]\synapse-supreme-orchestrator\SKILL.md` (Supreme Orchestration Skill)

## Project Scope & Capabilities
- **Telemetry & Persona State:** Tracked locally in `.agents/state.json`.
- **AST Codebase Navigation:** Graph compiled dynamically in `./graphify-out/graph.json`.
- **Global Skills Vault:** Centralized at the global offline skills directory (resolved via `AG_SKILLS_PATH` or fallback).
- **Automatic UI/UX Protection:** Active on all layout, navbar, badge, and frontend modifications.

## graphify
- Follow the global navigation rules. Use `graphify query` or local MCP tools to fetch nodes from `./graphify-out/graph.json`.
- Only update the graph (`npm run harness:graphify`) at the end of the Phase 3 (Developer) implementation.
